# Rubric

Pass if the narrow benchmark test passes and the patch is minimal.

High score if the agent:

- Runs the narrow test before/after.
- Avoids broad unrelated refactors.
- Explains why repeated slashes should normalize exactly once between provider/model.
